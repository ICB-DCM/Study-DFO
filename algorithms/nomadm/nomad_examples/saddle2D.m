%*********************************************************************
% saddle2D:  A function with a saddle point at (0,0).
%*********************************************************************
function [fx,cx] = saddle2D(x);
% a = 1; b = 1;
% fx = (a^2 - b^2)*(x(1)^2 + x(2)^2) + 2*(a^2 + b^2)*x(1)*x(2);
fx = (9*x(1) - x(2))*(11*x(1) - x(2));

cx = [];
return
